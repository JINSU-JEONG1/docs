개인용 문서 생산 작업 공간.

DOCX, PPTX, XLSX, PDF 작업용 공통 스킬은 `skills/`에 보관한다. Claude와 Codex의 스킬 탐색 경로는 각각 `.claude/skills`, `.agents/skills`이며 두 경로 모두 같은 `skills/` 폴더를 가리킨다.

새로 클론한 후 다음 명령을 한 번 실행해 연결을 생성한다.

```powershell
.\setup-skills.ps1
```

